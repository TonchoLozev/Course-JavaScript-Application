{{#each chirps}}
    <article class="chirp" data-id="{{_id}}">
        <div class="titlebar">
            <a href="#" class="chirp-author">{{author}}</a>
            <span class="chirp-time">{{time}}</span>
            <a href="#" class="deleteBtn">delete</a>
        </div>
        <p>{{text}}</p>
    </article>
{{/each}}